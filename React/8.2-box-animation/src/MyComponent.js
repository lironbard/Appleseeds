const MyComponent = () => {
  const buttonText1 = "Important!";

  return <button style={{ backgroundColor: "blue", color: "white", fontWeight: "bold" }}>{buttonText1}</button>;
};

export default MyComponent;
